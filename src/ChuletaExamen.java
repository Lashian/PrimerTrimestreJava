import java.util.Scanner;

public class ChuletaExamen {
/*	
	//Imprimir array
	public static void printArray(int[]miarray) {
		for (int i = 0; i <miarray.length; i++) {
			System.out.println((i+1) + "\t" + miarray[i] + " times.");
		}
	}

	
	//Copiar array
	public static void copyArray(){
	 int[] c = {1, 2, 3};       
	 int[] d = new int[c.length];
     for (int i = 0; i < d.length; i++) {
        d[i] = c[i];
     }
   }
	
	//Recorrer el array sustituyendo numeros (visto en el ahorcado)
	int[]randomWord = new int[5];
	int[] user_Word = new int[randomWord.length];
	for (int i = 0; i < user_Word.length; i++) {
		user_Word[i] = '_';
	}
	
	//Ejemplo menu 
	public static int menu() {
		Scanner input = new Scanner(System.in);
		int op = 0;
		System.out.println("Menu");
		System.out.println("\t1.- Print deck.");
		System.out.println("\t2.- Shuffle deck.");
		System.out.println("\t3.- Deal card.");
		System.out.println("\t4.- Exit.");
		System.out.println("\t\tOption:");
		op = input.nextInt();

		return op;
	}
	
	//Metodo del menu anterior
	
	int op;
	do {
		op = menu();
		switch (op) {
		case 1:
			printDeck(deck);
			break;
		case 2:
			shuffle(deck);
			break;
		case 3:
			int card = dealCard(deck);
			if (card != 0)
				System.out.println(cardToString(card));
			else {
				System.out.println("Shuffling...Choose 3 again");
				top = 0;
				shuffle(deck);
			}
		}

	} while (op != 4);
}
	
	
	*/
}
